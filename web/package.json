{
  "scripts": {
    "tailwind:build": "tailwindcss -i ./input.css -o ./assets/tailwind.css --minify",
    "tailwind:watch": "bun run tailwind:build -- --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.17",
    "tailwindcss": "^4.1.17"
  }
}
